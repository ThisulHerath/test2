# Base URL including the /api path supplied by the backend team
VITE_API_BASE_URL=http://localhost:5000/api
# Set to false once the backend is running to surface connection errors instead of sample data
VITE_USE_MOCK_DATA=true
